'use strict';

/**
 * acceleration-of-sale router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::acceleration-of-sale.acceleration-of-sale');
