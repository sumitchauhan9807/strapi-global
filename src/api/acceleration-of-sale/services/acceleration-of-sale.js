'use strict';

/**
 * acceleration-of-sale service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::acceleration-of-sale.acceleration-of-sale');
