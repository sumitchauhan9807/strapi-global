'use strict';

/**
 * acceleration-of-sale controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::acceleration-of-sale.acceleration-of-sale');
